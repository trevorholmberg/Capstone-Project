<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SignLanguageApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SignLanguageApp" parent="Base.Theme.SignLanguageApp" />

    <style name="Theme.App.starting" parent="Theme.SplashScreen">
        <!--Set the splash screen background, animated icon, and animation duration.-->
        <item name="windowSplashScreenBackground">@color/gold</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/wcu1</item>
        <item name="windowSplashScreenAnimationDuration">300</item> <!--# Required for animated icons-->
        <item name="postSplashScreenTheme">@style/Base.Theme.SignLanguageApp</item>
    </style>
</resources>